diff --git a/android/src/main/java/com/capacitorjs/community/plugins/bluetoothle/DeviceScanner.kt b/android/src/main/java/com/capacitorjs/community/plugins/bluetoothle/DeviceScanner.kt
--- a/android/src/main/java/com/capacitorjs/community/plugins/bluetoothle/DeviceScanner.kt
+++ b/android/src/main/java/com/capacitorjs/community/plugins/bluetoothle/DeviceScanner.kt
@@ -61,11 +61,21 @@
                     return
                 }
             }
+            val deviceName = try {
+                result.device.name ?: result.scanRecord?.deviceName
+            } catch (e: SecurityException) {
+                null
+            }
+            if (showDialog && (deviceName.isNullOrBlank() || deviceName.equals("Unknown", true))) {
+                // Hide unnamed peripherals: users pick their diffuser by name,
+                // and raw MAC addresses are noise in the chooser.
+                return
+            }
             val isNew = deviceList.addDevice(result.device)
             if (showDialog) {
                 if (isNew) {
                     dialogHandler?.post {
-                        deviceStrings.add("[${result.device.address}] ${result.device.name ?: "Unknown"}")
+                        deviceStrings.add(deviceName ?: result.device.address)
                         adapter?.notifyDataSetChanged()
                     }
                 }
@@ -75,6 +85,19 @@
                 }
             }
         }
+
+        override fun onScanFailed(errorCode: Int) {
+            super.onScanFailed(errorCode)
+            Logger.error(TAG, "Bluetooth scan failed with Android error code $errorCode", null)
+            stopScanning()
+            dialogHandler?.post {
+                dialog?.dismiss()
+            }
+            savedCallback?.invoke(
+                ScanResponse(false, "Bluetooth scan failed (Android error $errorCode). Restart Bluetooth and try again.", null)
+            )
+            savedCallback = null
+        }
     }
 
     fun startScanning(
@@ -93,14 +116,17 @@
         deviceStrings.clear()
         deviceList.clear()
         if (!isScanning) {
+            // Prepare the dialog before starting the scanner. Some Android
+            // chipsets deliver their first advertisement immediately.
+            if (showDialog) {
+                dialogHandler = Handler(Looper.getMainLooper())
+                showDeviceList()
+            }
             setTimeoutForStopScanning()
             Logger.debug(TAG, "Start scanning.")
             isScanning = true
             bluetoothLeScanner?.startScan(scanFilters, scanSettings, scanCallback)
-            if (showDialog) {
-                dialogHandler = Handler(Looper.getMainLooper())
-                showDeviceList()
-            } else {
+            if (!showDialog) {
                 savedCallback?.invoke(
                     ScanResponse(
                         true, "Started scanning.", null
@@ -140,6 +166,7 @@
         dialogHandler?.post {
             val builder = AlertDialog.Builder(context)
             builder.setTitle(displayStrings.scanning)
+            builder.setMessage("Double tap your diffuser button to enter pairing mode and edit settings")
             builder.setCancelable(true)
             adapter = ArrayAdapter(
                 context, android.R.layout.simple_selectable_list_item, deviceStrings
@@ -173,6 +200,11 @@
             }
             dialog = builder.create()
             dialog?.show()
+            // Make "Cancel" unmistakable: red, square, no rounded chrome.
+            dialog?.getButton(AlertDialog.BUTTON_NEGATIVE)?.apply {
+                setTextColor(android.graphics.Color.parseColor("#E5484D"))
+                background = null
+            }
         }
     }
 
diff --git a/ios/Sources/BluetoothLe/Plugin.swift b/ios/Sources/BluetoothLe/Plugin.swift
--- a/ios/Sources/BluetoothLe/Plugin.swift
+++ b/ios/Sources/BluetoothLe/Plugin.swift
@@ -30,6 +30,7 @@ public class BluetoothLe: CAPPlugin, CAPBridgedPlugin {
         CAPPluginMethod(name: "getDevices", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "discoverServices", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "getConnectedDevices", returnType: CAPPluginReturnPromise),
+        CAPPluginMethod(name: "isDeviceConnected", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "connect", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "createBond", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "isBonded", returnType: CAPPluginReturnPromise),
@@ -274,6 +275,13 @@ public class BluetoothLe: CAPPlugin, CAPBridgedPlugin {
         }
     }
 
+    @objc func isDeviceConnected(_ call: CAPPluginCall) {
+        DispatchQueue.main.async {
+            guard let device = self.getDevice(call, checkConnection: false) else { return }
+            call.resolve(["value": device.isConnected()])
+        }
+    }
+
     @objc func connect(_ call: CAPPluginCall) {
         DispatchQueue.main.async {
             guard let device = self.getDevice(call, checkConnection: false) else { return }
